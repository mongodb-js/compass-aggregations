import { STAGE_OPERATORS } from 'mongodb-ace-autocompleter';

// const operatorsMappedToNodes = {

// };

const operators = [
  ...STAGE_OPERATORS
];

// operators.push({

// });

export default operators;
